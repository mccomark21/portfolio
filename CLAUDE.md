@AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for this repository. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage uses the default canonical label vocabulary unless you remap it later. See `docs/agents/triage-labels.md`.

### Domain docs

This repo is configured as a single-context layout, reading root `CONTEXT.md` and `docs/adr/` when present. See `docs/agents/domain.md`.
